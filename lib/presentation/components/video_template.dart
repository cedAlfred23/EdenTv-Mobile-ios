import 'package:flutter/material.dart';

class VideoTemplate extends StatelessWidget {
  const VideoTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
